interface ResidentialQuarter {
    id: number
    location: string
    code: string
    policy: string

    max_cars_registrations: number
    quarter_name: string
    guest_free_days: number
    max_cars_by_apartment: number
}

export default ResidentialQuarter